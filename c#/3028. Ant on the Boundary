public class Solution {
    public int ReturnToBoundaryCount(int[] nums) {
     int total=0;
     int numOfTimes=0;
     for(int i=0;i<nums.Length;i++){
      total+=nums[i];
      if(total==0) numOfTimes++;
     }
     return numOfTimes;   
    }
}
