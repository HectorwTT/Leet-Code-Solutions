public class Solution {
    public bool ValidDigit(int n, int x) {
      string number="" +n;
      if(number[0]==48+x)return false;
      int count=0;
      for(int i=1;i<number.Length;i++){
       if(number[i]==48+x) return true;
      } 
      return false;   
    }
}
