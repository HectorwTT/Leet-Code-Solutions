class Solution {
public:
    int firstMatchingIndex(string s) {
     int left=0;
     int right=s.size()-1;
     while(left<=right){
      char ch1=s[left++];
      char ch2=s[right--];
      if(ch1==ch2)return left-1;  
     } 
     return -1;    
    }
};
