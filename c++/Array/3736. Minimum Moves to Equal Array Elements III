class Solution {
public:
    int minMoves(vector<int>& nums) {
     int answer=0;
     std::sort(nums.begin(),nums.end());
     int max=nums[nums.size()-1];
     for(int i=0;i<nums.size();i++)answer+=max-nums[i];
     return answer;    
    }
};
