class Solution {
public:
    int minimumDifference(vector<int>& nums, int k) {
      int answer=std::pow(10,5)+1;
      std::sort(nums.begin(),nums.end());
      for(int i=0;i<=nums.size()-k;i++){
       answer=std::min(answer,nums[i+k-1]-nums[i]); 
      } 
      return answer;   
    }
};
