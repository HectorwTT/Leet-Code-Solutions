class Solution {
public:
    int earliestTime(vector<vector<int>>& tasks) {
     int answer=std::pow(10,9);  
     for(int i=0;i<tasks.size();i++){
      int finish=tasks[i][0]+tasks[i][1];
      answer=std::min(answer,finish);        
     } 
     return answer;   
    }
};
