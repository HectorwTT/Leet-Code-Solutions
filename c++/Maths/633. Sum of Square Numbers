class Solution {
public:
    bool judgeSquareSum(int c) {
      for (long a = 0; a * a <= c; a++) {
            double b = std::pow(c - a * a,0.5);
            if (b == (int) b)
                return true;
        }
        return false;    
    }
};
