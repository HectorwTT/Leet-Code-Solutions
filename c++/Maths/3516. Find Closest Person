class Solution {
public:
    int findClosest(int x, int y, int z) {
     if(std::abs(y-z)==std::abs(x-z))return 0;   
     else if(std::abs(y-z)<std::abs(x-z))return 2;
     else return 1;   
    }
};
