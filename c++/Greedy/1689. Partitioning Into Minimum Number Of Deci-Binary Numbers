class Solution {
public:
    int minPartitions(string n) {
      int answer=0;  
      for(int i=0;i<n.size();i++){
        char ch=n[i];
        int digit=ch-48;
        answer=std::max(digit,answer);
      }
      return answer;     
    }
};
