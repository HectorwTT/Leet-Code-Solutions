class Solution {
    public void reverseString(char[] s) {
     int left=0;
     int right=s.length-1;
     while(left<right){
         char lefttmp= s[left];
         char righttmp=s[right];
         s[left]=righttmp;
         s[right]=lefttmp;
         left++;
         right--;
     }
     
    }
}
