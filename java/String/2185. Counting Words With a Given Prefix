class Solution {
    public int prefixCount(String[] words, String pref) {
        int total=0;
        for(int i=0;i<words.length;i++){
         if(words[i].indexOf(pref)==0)total++;
        }
        return total;
    }
}
