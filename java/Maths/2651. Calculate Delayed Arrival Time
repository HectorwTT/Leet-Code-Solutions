class Solution {
    public int findDelayedArrivalTime(int arrivalTime, int delayedTime) {
      int newTime= arrivalTime+delayedTime;
      if(newTime<24)return newTime;
      else if(newTime==24)return 0;
      else return newTime%24;  
    }
}
