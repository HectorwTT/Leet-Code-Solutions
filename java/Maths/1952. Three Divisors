class Solution {
    public boolean isThree(int n) {
        int number=0;
        for(int i=1;i<=n;i++){
         if(n%i==0)number++;
         if(number>3)return false;
        }
        return number==3;
    }
}
