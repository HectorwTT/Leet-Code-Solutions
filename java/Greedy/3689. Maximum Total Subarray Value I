class Solution {
    public long maxTotalValue(int[] nums, int k) {
       long min=(long)Math.pow(10,9);
       long max=-1;
       for(int i=0;i<nums.length;i++){
        min=Math.min(1L*nums[i],min);
        max=Math.max(1L*nums[i],max);
       }
       return k*(max-min); 
    }
}
