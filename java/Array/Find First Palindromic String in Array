class Solution {
    public String firstPalindrome(String[] words) {
        for (int i=0;i<words.length;i++){
            StringBuffer sb= new StringBuffer(words[i]);
            sb.reverse();
            if(sb.toString().equals(words[i]))return words[i];
        }
        return "";
    }
}
