var validDigit = function(n, x) {
      let number="" +n;
      if(number.charCodeAt(0)==48+x)return false;
      let count=0;
      for(let i=1;i<number.length;i++){
       if(number.charCodeAt(i)==48+x) return true;
      } 
      return false;   
};
