var minCostToMoveChips = function(position) {
 let numOfEven=0;
 let numOfOdd=0;
 for(let i=0;i<position.length;i++){
  if(position.at(i)%2==0)numOfEven++;
  else numOfOdd++;
 }
 return Math.min(numOfEven,numOfOdd);   
};
