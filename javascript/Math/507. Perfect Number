var checkPerfectNumber = function(num) {
 let divisor=1;
 let sumOfDivisors=0;
 while(divisor<= num/2){
  if(num%divisor==0) sumOfDivisors+=divisor;
  divisor++;
 }
 return sumOfDivisors==num;    
};
