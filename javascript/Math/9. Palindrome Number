var isPalindrome = function(x) {
 let string=""+x;
 let left=0;
 let right=string.length-1;
 while(left<right){
  var leftCh= string.charAt(left);
  var rightCh=string.charAt(right);
  if(leftCh!=rightCh)return false;
  left++;
  right--;
 }
 return true;
};
